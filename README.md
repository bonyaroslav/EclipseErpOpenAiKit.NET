# EclipseErpOpenAiKit.NET
Epicor Eclipse ERP ↔ ChatGPT integration kit (.NET). Contract-first OpenAPI clients, session lifecycle, draft-only safety guardrails, governance (allowlists/redaction), correlation + audit. Runs E2E locally with Mock Eclipse; enable real OpenAI tool-calling with an API key.
