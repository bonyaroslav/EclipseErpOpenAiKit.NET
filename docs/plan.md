# Detailed plan

This repository uses `plan.md` at the repository root as the active source of truth for MVP scope, scenarios, and acceptance criteria.

Use `../plan.md` for implementation status and next steps.
