# Domain documentation

This is a single-context repository.

Before relevant work, read `CONTEXT.md` and applicable ADRs under
`docs/adr/` when they exist. Proceed silently when they do not exist.

Create domain documents lazily; do not require them for routine work.
Use established glossary terms consistently and flag conflicts with ADRs.
