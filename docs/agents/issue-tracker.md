# Local issue tracker

Specs and implementation tickets live under `.scratch/<feature>/`.

- Spec: `.scratch/<feature>/spec.md`
- Tickets, when needed: `.scratch/<feature>/issues/<NN>-<slug>.md`
- State: one `Status:` line near the top
- Create only the files required for the current work
